<%= form_for @post do |f| %>
  Title:</br>
  <%= f.text_field :title %></br>
  Body:</br>
  <%= f.text_area :body, size: "60x12" %></br>

  <%= f.submit (@post.persisted? ? 'Update post' : 'Create post') %>

<% end %>	